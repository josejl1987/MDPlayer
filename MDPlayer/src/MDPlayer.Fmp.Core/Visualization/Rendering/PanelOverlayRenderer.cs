namespace Fmp.Core.Visualization.Rendering;

// File retained as the partial-type anchor; implementation is grouped in the
// named mechanical partial files. The overlay interface is declared here: the
// CPU renderer already exposes every member with matching signatures, so this
// declaration is purely structural — no drawing behavior changes.
internal sealed partial class PanelOverlayRenderer : IFrameOverlayRenderer
{

}
